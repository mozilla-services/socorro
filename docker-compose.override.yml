---
# define volumes in docker-compose.override.yml so that can be ignored in CI
services:
  app:
    volumes:
      - .:/app
      - /app/webapp/node_modules
  test:
    volumes:
      - .:/app
      - /app/webapp/node_modules

  processor:
    volumes:
      - .:/app

  crontabber:
    volumes:
      - .:/app

  webapp:
    volumes:
      - .:/app
      - /app/webapp/node_modules

  stage_submitter:
    volumes:
      - .:/app

  collector:
    volumes:
      - .:/socorro

  fakecollector:
    volumes:
      - .:/app

  symbolsserver:
    volumes:
      - .:/app
