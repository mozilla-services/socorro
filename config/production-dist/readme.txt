For a distributed production environment where managing
configuration is split between ops and dev, move these 
ini files into the config directory.  The dev team can
edit them in that location at will.

the files in etc-socorro should eventually get put into
/etc/socorro where the ops team can edit them at will.
