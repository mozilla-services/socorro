=======================
Crash storage: Postgres
=======================

The processor saves raw crash data to Postgres. That data is then moved around
to a bunch of other tables for analysis.

Current status: This is going away 2018h1.
